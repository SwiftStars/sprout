#!/usr/local/bin/sprout check --location

% projectname: Sprout
% builtcli: .build/release/sprout
% giturl: ssh://git@github.com/SwiftStars/sprout.git
% snapto: branch~master
# % snapto: latest

% build {

    swift build -c release

% }

% install {

    install->bin .build/release/sprout

}
